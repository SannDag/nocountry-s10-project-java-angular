---
server:
  port: 8088
eureka:
  client:
    service-url:
      defaultZone=: http://localhost:8761/eureka
spring:
  main:
    banner-mode: off
  application:
    name: api-gateway
  cloud:
    gateway:
      #discovery:
        #locator:
          #enabled: true
      routes:

management:
  tracing:
    sampling:
      probability: 1.0
...