---
server:
  error:
    include-stacktrace: never
  port: 5555

spring:
  main:
    banner-mode: off
  application:
    name: api-gateway
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
      discovery:
        locator:
          enabled: true
      routes:
        - id: security
          uri: lb://security
          predicates:
            - Path=/**
        - id: eureka-server
          uri: https://ssdfsdf.azurewebsites.net
          predicates:
            - Path=/**

eureka:
  client:
    enabled: true
    service-url:
      #defaultZone: https://ssdfsdf.azurewebsites.net:8761/eureka/
      defaultZone: https://ssdfsdf.azurewebsites.net/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

#security:
  #service:
    #url: http://localhost:8081
...